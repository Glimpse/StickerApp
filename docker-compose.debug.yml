version: '2.1'

services:
  stickerapp:
    image: stickerapp
    build: .
    environment:
      NODE_ENV: development
      MONGODB_URL: 
    ports:
      - 3000:3000
      - 9229:9229
    volumes:
      - .:/usr/src/app
    command: node --inspect server/index.js